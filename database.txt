-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.alumni (
  id bigint NOT NULL DEFAULT nextval('alumni_id_seq'::regclass),
  usn text UNIQUE,
  full_name text NOT NULL,
  graduation_year integer,
  current_company text,
  current_designation text,
  current_work_location text,
  personal_email text,
  phone_number text,
  linkedin text,
  other_links jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT alumni_pkey PRIMARY KEY (id),
  CONSTRAINT fk_alumni_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.auth_refresh_tokens (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_login_id bigint NOT NULL,
  token_hash text NOT NULL,
  jti uuid NOT NULL,
  expires_at timestamp with time zone NOT NULL,
  revoked_at timestamp with time zone,
  replaced_by uuid,
  user_agent text,
  ip text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT auth_refresh_tokens_pkey PRIMARY KEY (id),
  CONSTRAINT auth_refresh_tokens_user_login_id_fkey FOREIGN KEY (user_login_id) REFERENCES public.user_login(id)
);
CREATE TABLE public.companies (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  company_name text NOT NULL UNIQUE,
  description text,
  company_type text,
  address text,
  website text,
  linkedin text,
  remarks ARRAY,
  company_logo_link text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT companies_pkey PRIMARY KEY (id)
);
CREATE TABLE public.contacts (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  company_id bigint NOT NULL,
  contact_name text,
  email text,
  phone_number text,
  role_title text,
  remarks text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT contacts_pkey PRIMARY KEY (id),
  CONSTRAINT fk_contacts_company FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.events (
  id bigint NOT NULL DEFAULT nextval('events_id_seq'::regclass),
  title text NOT NULL,
  type USER-DEFINED NOT NULL,
  details text,
  event_datetime timestamp with time zone,
  images jsonb,
  attachments jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT events_pkey PRIMARY KEY (id)
);
CREATE TABLE public.job_offers (
  id integer NOT NULL DEFAULT nextval('job_offers_id_seq'::regclass),
  usn text NOT NULL,
  job_type text,
  internship_duration text,
  internship_stipend text,
  ctc_min_lpa text,
  ctc_max_lpa text,
  ctc_variable_pay text,
  designation text,
  offer_letter_status text,
  remarks text,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  final_interview_status text NOT NULL,
  company_id bigint,
  CONSTRAINT job_offers_pkey PRIMARY KEY (id),
  CONSTRAINT fk_job_offers_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn),
  CONSTRAINT fk_job_offers_company_id FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.majors (
  id bigint NOT NULL DEFAULT nextval('majors_id_seq'::regclass),
  program_id bigint NOT NULL,
  name text NOT NULL,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT majors_pkey PRIMARY KEY (id),
  CONSTRAINT fk_majors_program FOREIGN KEY (program_id) REFERENCES public.programs(id)
);
CREATE TABLE public.minors (
  id bigint NOT NULL DEFAULT nextval('minors_id_seq'::regclass),
  name text NOT NULL,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  school_name text,
  CONSTRAINT minors_pkey PRIMARY KEY (id),
  CONSTRAINT fk_minors_school_name FOREIGN KEY (school_name) REFERENCES public.schools(name)
);
CREATE TABLE public.placements_drives (
  id bigint NOT NULL DEFAULT nextval('placements_id_seq'::regclass),
  company_remarks text,
  tpo text,
  year integer,
  eligibility_academics jsonb,
  type_of_hiring text,
  job_type text,
  ctc_structure jsonb,
  job_location text,
  onboarded_date date,
  last_date_to_registration text,
  no_shortlisted integer,
  offer_letter_status text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  institution_id bigint,
  event_datetime timestamp with time zone,
  job_description text,
  stipend_structure jsonb,
  school_id bigint,
  program_id bigint,
  specialization_id bigint,
  number_of_openings integer,
  number_of_registrations integer,
  placement_status text,
  company_id bigint,
  CONSTRAINT placements_drives_pkey PRIMARY KEY (id),
  CONSTRAINT fk_placements_institution FOREIGN KEY (institution_id) REFERENCES public.universities(id),
  CONSTRAINT fk_placements_school FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT fk_placements_program FOREIGN KEY (program_id) REFERENCES public.programs(id),
  CONSTRAINT fk_placements_specialization FOREIGN KEY (specialization_id) REFERENCES public.specializations(id),
  CONSTRAINT fk_placements_company_id FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.programs (
  id bigint NOT NULL DEFAULT nextval('programs_id_seq'::regclass),
  name text NOT NULL,
  graduation_level text NOT NULL CHECK (graduation_level = ANY (ARRAY['UG'::text, 'PG'::text])),
  remarks text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  school_name text,
  CONSTRAINT programs_pkey PRIMARY KEY (id),
  CONSTRAINT fk_programs_school_name FOREIGN KEY (school_name) REFERENCES public.schools(name)
);
CREATE TABLE public.roles (
  id bigint NOT NULL DEFAULT nextval('roles_id_seq'::regclass),
  name text NOT NULL UNIQUE,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT roles_pkey PRIMARY KEY (id)
);
CREATE TABLE public.schools (
  id bigint NOT NULL DEFAULT nextval('schools_id_seq'::regclass),
  university_id bigint NOT NULL,
  name text NOT NULL UNIQUE,
  abbreviation text NOT NULL,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT schools_pkey PRIMARY KEY (id),
  CONSTRAINT fk_schools_university FOREIGN KEY (university_id) REFERENCES public.universities(id)
);
CREATE TABLE public.specializations (
  id bigint NOT NULL DEFAULT nextval('specializations_id_seq'::regclass),
  program_id bigint NOT NULL,
  name text NOT NULL,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT specializations_pkey PRIMARY KEY (id),
  CONSTRAINT fk_specializations_program FOREIGN KEY (program_id) REFERENCES public.programs(id)
);
CREATE TABLE public.student_certifications (
  id bigint NOT NULL DEFAULT nextval('student_certifications_id_seq'::regclass),
  usn text NOT NULL,
  title text NOT NULL,
  organization text NOT NULL,
  certification_type text,
  skills text,
  score text,
  issue_date date,
  expiry_date date,
  proof_document text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_certifications_pkey PRIMARY KEY (id),
  CONSTRAINT fk_certification_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.student_education_history (
  id bigint NOT NULL DEFAULT nextval('student_education_history_id_seq'::regclass),
  usn text NOT NULL,
  education_level text NOT NULL CHECK (education_level = ANY (ARRAY['10TH'::text, '12TH'::text, 'DIPLOMA'::text, 'GRADUATION'::text, 'POST_GRADUATION'::text, 'OTHER'::text, 'EDUCATION_GAP'::text])),
  institute_name text,
  city text,
  board text,
  year_of_passing integer,
  result numeric,
  result_type text CHECK (result_type = ANY (ARRAY['PERCENTAGE'::text, 'CGPA'::text])),
  subjects text,
  marksheet_file text,
  gap_type text CHECK (gap_type = ANY (ARRAY['12TH_TO_GRADUATION'::text, 'DIPLOMA_TO_GRADUATION'::text, 'GRADUATION_TO_POST_GRADUATION'::text])),
  gap_duration_months integer,
  gap_reason text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_education_history_pkey PRIMARY KEY (id),
  CONSTRAINT fk_student_education_history_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.student_extra_curricular_activities (
  id bigint NOT NULL DEFAULT nextval('student_extra_curricular_activities_id_seq'::regclass),
  usn text NOT NULL,
  activity_name text NOT NULL,
  activity_type text NOT NULL,
  role text,
  organization text,
  start_date date,
  end_date date,
  achievements text,
  skills text,
  description text,
  proof_document text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_extra_curricular_activities_pkey PRIMARY KEY (id),
  CONSTRAINT fk_extra_curricular_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.student_internships (
  id bigint NOT NULL DEFAULT nextval('student_internships_id_seq'::regclass),
  usn text NOT NULL,
  job_role text NOT NULL,
  organization text NOT NULL,
  organization_details text,
  duration_months integer,
  start_date date,
  end_date date,
  location text,
  stipend numeric,
  skills text,
  description text,
  mentor_name text,
  proof_document text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_internships_pkey PRIMARY KEY (id),
  CONSTRAINT fk_internship_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.student_other_experiences (
  id bigint NOT NULL DEFAULT nextval('student_other_experiences_id_seq'::regclass),
  usn text NOT NULL,
  title text NOT NULL,
  organization text,
  start_date date,
  end_date date,
  location text,
  skills text,
  description text,
  proof_document text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_other_experiences_pkey PRIMARY KEY (id),
  CONSTRAINT fk_other_experience_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.student_parent_details (
  id bigint NOT NULL DEFAULT nextval('student_parent_details_id_seq'::regclass),
  usn text NOT NULL,
  parent_type text NOT NULL CHECK (parent_type = ANY (ARRAY['Father'::text, 'Mother'::text, 'Guardian'::text])),
  name text NOT NULL,
  occupation text,
  organisation text,
  email text,
  phone_country_code text,
  phone_number text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_parent_details_pkey PRIMARY KEY (id),
  CONSTRAINT fk_parent_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.student_placement_process (
  id bigint NOT NULL DEFAULT nextval('placement_student_progress_id_seq'::regclass),
  placement_drive_id bigint NOT NULL,
  usn text NOT NULL,
  is_eligible text NOT NULL DEFAULT '-'::text,
  registration_status text NOT NULL DEFAULT '-'::text,
  approved_status text NOT NULL DEFAULT '-'::text,
  oa_status text NOT NULL DEFAULT '-'::text,
  gd_status text NOT NULL DEFAULT '-'::text,
  technical_round_status text NOT NULL DEFAULT '-'::text,
  interview_status text NOT NULL DEFAULT '-'::text,
  hr_round_status text NOT NULL DEFAULT '-'::text,
  final_select_status text NOT NULL DEFAULT '-'::text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  malpractice boolean,
  remarks text,
  CONSTRAINT student_placement_process_pkey PRIMARY KEY (id),
  CONSTRAINT fk_psp_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn),
  CONSTRAINT process_placement_drive_id_fkey FOREIGN KEY (placement_drive_id) REFERENCES public.placements_drives(id)
);
CREATE TABLE public.student_profile_details (
  usn text NOT NULL,
  brief_summary text,
  key_expertise text,
  hobbies_interests text,
  career_objective text,
  dream_package numeric,
  dream_company text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_profile_details_pkey PRIMARY KEY (usn),
  CONSTRAINT fk_profile_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.student_projects (
  id bigint NOT NULL DEFAULT nextval('student_projects_id_seq'::regclass),
  usn text NOT NULL,
  title text NOT NULL,
  description text,
  skills text,
  project_link text,
  snaps text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  mentor_name text,
  CONSTRAINT student_projects_pkey PRIMARY KEY (id),
  CONSTRAINT fk_project_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.student_publications (
  id bigint NOT NULL DEFAULT nextval('student_publications_id_seq'::regclass),
  usn text NOT NULL,
  title text NOT NULL,
  publication_name text,
  publication_type text NOT NULL,
  publication_date date,
  author_count integer CHECK (author_count > 0),
  mentor_name text,
  skills text,
  description text,
  evidence_document text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_publications_pkey PRIMARY KEY (id),
  CONSTRAINT fk_publication_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.student_semester_academics (
  id bigint NOT NULL DEFAULT nextval('student_semester_academics_id_seq'::regclass),
  usn text NOT NULL,
  academic_year integer NOT NULL,
  semester integer NOT NULL CHECK (semester >= 1 AND semester <= 8),
  result_in_sgpa numeric NOT NULL,
  closed_backlogs integer DEFAULT 0,
  live_backlogs integer DEFAULT 0,
  provisional_result_upload_link ARRAY NOT NULL,
  uploaded_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_semester_academics_pkey PRIMARY KEY (id),
  CONSTRAINT fk_semester_academics_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.student_trainings (
  id bigint NOT NULL DEFAULT nextval('student_trainings_id_seq'::regclass),
  usn text NOT NULL,
  title text NOT NULL,
  institution text NOT NULL,
  training_type text,
  start_date date,
  end_date date,
  skills text,
  description text,
  proof_document text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_trainings_pkey PRIMARY KEY (id),
  CONSTRAINT fk_training_student FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.students_personal_details (
  usn text NOT NULL,
  full_name text NOT NULL,
  gender text CHECK (gender = ANY (ARRAY['MALE'::text, 'FEMALE'::text, 'Other'::text])),
  date_of_birth date,
  blood_group text,
  marital_status text CHECK (marital_status = ANY (ARRAY['Single'::text, 'Married'::text])),
  specially_abled boolean DEFAULT false,
  languages text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  profile_image text,
  school_name text,
  year_of_joining integer NOT NULL CHECK (year_of_joining > 2020),
  program_id bigint,
  specialization_id bigint,
  major_id bigint,
  minor_id bigint,
  is_profile_locked boolean DEFAULT false,
  college_email text UNIQUE CHECK (college_email ~* '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$'::text),
  personal_email text,
  phone_country_code text,
  phone_number text,
  links jsonb,
  CONSTRAINT students_personal_details_pkey PRIMARY KEY (usn),
  CONSTRAINT fk_student_program FOREIGN KEY (program_id) REFERENCES public.programs(id),
  CONSTRAINT fk_student_specialization FOREIGN KEY (specialization_id) REFERENCES public.specializations(id),
  CONSTRAINT fk_student_major FOREIGN KEY (major_id) REFERENCES public.majors(id),
  CONSTRAINT fk_student_minor FOREIGN KEY (minor_id) REFERENCES public.minors(id),
  CONSTRAINT fk_student_school_name FOREIGN KEY (school_name) REFERENCES public.schools(name)
);
create index IF not exists idx_students_usn on public.students_personal_details using btree (usn) TABLESPACE pg_default;
CREATE TABLE public.universities (
  id bigint NOT NULL DEFAULT nextval('universities_id_seq'::regclass),
  name text NOT NULL UNIQUE,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  logo text,
  CONSTRAINT universities_pkey PRIMARY KEY (id)
);
CREATE TABLE public.user_login (
  id bigint NOT NULL DEFAULT nextval('user_login_id_seq'::regclass),
  mail text UNIQUE,
  role_id bigint NOT NULL,
  password_hash text NOT NULL,
  is_active boolean DEFAULT true,
  last_login_at timestamp with time zone,
  failed_login_attempts integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  usn text,
  deleted_at timestamp with time zone,
  CONSTRAINT user_login_pkey PRIMARY KEY (id),
  CONSTRAINT fk_login_role FOREIGN KEY (role_id) REFERENCES public.roles(id),
  CONSTRAINT user_login_usn_fkey FOREIGN KEY (usn) REFERENCES public.students_personal_details(usn)
);
CREATE TABLE public.auth_refresh_tokens ( 
  id uuid not null default gen_random_uuid (), 
  user_login_id bigint not null, 
  token_hash text not null, 
  jti uuid not null, 
  expires_at timestamp with time zone not null, 
  revoked_at timestamp with time zone null, 
  replaced_by uuid null, 
  user_agent text null, 
  ip text null, 
  created_at timestamp with time zone not null default now(), 
  constraint auth_refresh_tokens_pkey primary key (id), 
  constraint unique_user_jti unique (user_login_id, jti), 
  constraint auth_refresh_tokens_user_login_id_fkey foreign KEY (user_login_id) references user_login (id) on delete CASCADE 
) TABLESPACE pg_default; 
 
create index IF not exists idx_auth_refresh_tokens_user_login_id on public.auth_refresh_tokens using btree (user_login_id) TABLESPACE pg_default; 
 
create index IF not exists idx_auth_refresh_tokens_token_hash on public.auth_refresh_tokens using btree (token_hash) TABLESPACE pg_default;
CREATE TABLE public.user_otp_verification (
  id bigint NOT NULL DEFAULT nextval('user_otp_verification_id_seq'::regclass),
  identifier character varying NOT NULL,
  otp_hash text NOT NULL,
  purpose character varying CHECK (purpose::text = ANY (ARRAY['REGISTRATION'::character varying, 'PASSWORD_RESET'::character varying, 'LOGIN'::character varying]::text[])),
  expires_at timestamp without time zone NOT NULL,
  verified boolean DEFAULT false,
  attempts integer DEFAULT 0,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT user_otp_verification_pkey PRIMARY KEY (id)
);
